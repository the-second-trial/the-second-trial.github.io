# build output
dist/

# generated standalone project artifacts (produced by `make build-<slug>`).
# These are rebuilt during deploy and must not be committed. Hand-authored
# assets in public/ (e.g. favicon.svg) are still tracked normally.
public/work/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS
.DS_Store

# astro
.astro/
